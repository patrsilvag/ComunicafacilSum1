package com.psilva.comunicafacil.ui.theme

import androidx.compose.ui.graphics.Color

// --- Primario (acción principal) ---
val AppPrimary = Color(0xFF2F5D8A)      // Azul actual
val AppOnPrimary = Color(0xFFFFFFFF)

// Contenedor primario (ideal para "éxito"/info y superficies destacadas)
val AppPrimaryContainer = Color(0xFFD8E3FF)   // Azul muy pálido
val AppOnPrimaryContainer = Color(0xFF001B3F) // Azul oscuro para texto legible

// --- Fondo / Superficies ---
val AppBackground = Color(0xFFF9FAFB)   // Fondo ligeramente más cálido
val AppOnBackground = Color(0xFF191C1E) // Negro suave para lectura

val AppSurface = Color(0xFFFFFFFF)      // Bloques / tarjetas
val AppOnSurface = Color(0xFF191C1E)

// --- Secundario (acción secundaria: Volver / Cerrar sesión) ---
val AppSecondary = Color(0xFF535F70)    // Neutro
val AppOnSecondary = Color(0xFFFFFFFF)

// Contenedor secundario
val AppSecondaryContainer = Color(0xFFDCE3F1)
val AppOnSecondaryContainer = Color(0xFF0F1B2A)

// --- Error (mensajes y validaciones) ---
val AppError = Color(0xFFBA1A1A)
val AppOnError = Color(0xFFFFFFFF)

val AppErrorContainer = Color(0xFFFFDAD6)
val AppOnErrorContainer = Color(0xFF410002)

// --- Bordes (OutlinedTextField) ---
val AppOutline = Color(0xFF808A96)
